// sequelize
// Still uses the mysql Database, but the code we write will be sort of different, Sequelize is a third party package that allows us work
// with Javascript methods and convient methods to create new element, to edit them, and to connect them.

// what is sequelize

// an object Relational mapping library
// which means,it does the heavy lifting (all the sql code behind the scenes for us) and parse it into js objects with methods we can call.
// we dont need to write sql on our own.

// core concepts
// offers us moodels to work with our database. 
// instances 
// Queries 
// Associations/ Relation: 
 // To connect different things/ model together e.g Has one: a model has on one relation or belongs to many: a model has many relation